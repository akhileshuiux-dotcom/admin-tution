import React from 'react';
import { motion } from 'framer-motion';

const Background = () => {
    return (
        <div className="fixed inset-0 z-0 bg-[#E2E8E4]" />
    );
};

export default Background;
